using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;
using UnityEngine.SceneManagement;

public class EndScr : MonoBehaviour {
/*
    public Text score;
    public PlayerScr player;

	// Use this for initialization
	void Start () {
        player.myAnimator.SetBool("PlayerMuerto", true);
        if (PlayerPrefs.GetInt(GameManager.LAST_RECORD) > PlayerPrefs.GetInt(GameManager.RECORD1) || 
            PlayerPrefs.GetInt(GameManager.LAST_RECORD) > PlayerPrefs.GetInt(GameManager.RECORD2) ||
            PlayerPrefs.GetInt(GameManager.LAST_RECORD) > PlayerPrefs.GetInt(GameManager.RECORD3)) {
            score.text = "Congratulations!! NEW RECORD: " + PlayerPrefs.GetInt(GameManager.LAST_RECORD);
        } else {
            score.text = "Record: " + PlayerPrefs.GetInt(GameManager.LAST_RECORD);
        }
        
        StartCoroutine(VolverAInicio());
	}

    private IEnumerator VolverAInicio() {
        yield return new WaitForSeconds(5);
        SceneManager.LoadScene("MenuPrincipalScn");
    }
	*/
}
